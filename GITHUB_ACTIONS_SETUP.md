# GitHub Actions 설정 가이드

## 🚀 개요
이 프로젝트는 GitHub Actions를 통해 자동 CI/CD를 제공합니다.

## 📁 워크플로우 구조
```
.github/
└── workflows/
    ├── ci.yml         # PR용 빌드/테스트
    └── deploy.yml     # dev 브랜치 자동 배포
```

## ⚙️ 필수 GitHub Secrets 설정

GitHub 저장소 Settings > Secrets and variables > Actions에서 다음을 설정하세요:

### SSH 연결 설정
```
SSH_HOST=13.125.59.135
SSH_USERNAME=ubuntu  
SSH_PRIVATE_KEY=<iku.pem 파일의 전체 내용>
```

### SSH_PRIVATE_KEY 설정 방법
1. `iku.pem` 파일을 텍스트 에디터로 열기
2. `-----BEGIN RSA PRIVATE KEY-----`부터 `-----END RSA PRIVATE KEY-----`까지 전체 복사
3. GitHub Secrets에 붙여넣기

## 🔄 자동 배포 플로우

### dev 브랜치 푸시 시:
1. **메모리 정리**: 미사용 Docker 리소스 삭제
2. **순차 배포**: 백엔드 → 프론트엔드 순서로 안전하게 배포
3. **헬스체크**: 각 서비스가 정상 동작하는지 확인
4. **상태 리포트**: 배포 완료 및 메모리 사용량 확인

### PR 생성 시:
1. **백엔드 테스트**: JUnit 테스트 실행
2. **프론트엔드 테스트**: TypeScript 체크, ESLint
3. **Docker 빌드 테스트**: 이미지 빌드 가능 여부 확인

## 🖥️ 배포된 서비스 접근

배포 완료 후 다음 주소로 접근 가능:
- **백엔드**: http://13.125.59.135:8080
- **프론트엔드**: http://13.125.59.135:3000
- **API 헬스체크**: http://13.125.59.135:8080/actuator/health

## 🚨 메모리 최적화 특징

현재 서버 메모리(1.9GB)를 고려한 안전한 배포:
- ✅ 순차적 컨테이너 재시작
- ✅ 배포 전 Docker 리소스 정리  
- ✅ 각 단계별 헬스체크
- ✅ 메모리 사용량 모니터링

## 🔧 사용법

1. **자동 배포**: `dev` 브랜치에 코드 푸시
   ```bash
   git push origin dev
   ```

2. **수동 확인**: Actions 탭에서 배포 진행 상황 확인

3. **배포 실패 시**: Actions 로그에서 상세 오류 확인

## 📊 모니터링

배포 과정에서 다음 정보를 자동으로 체크:
- 메모리 사용량 (배포 전/후)
- 컨테이너 상태
- 서비스 헬스체크 결과
- 배포 소요 시간

---

## ⚠️ 주의사항

1. **메모리 부족 위험**: 현재 서버 메모리가 타이트하므로 동시 빌드를 피함
2. **배포 시간**: 안전한 순차 배포로 인해 3-5분 소요
3. **롤백**: 문제 발생 시 수동으로 이전 커밋으로 롤백 필요