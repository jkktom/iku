# Frontend Dev 브랜치용 Dockerfile (Development)
FROM node:20-alpine

WORKDIR /app

# 패키지 파일들 복사
COPY package*.json ./

# 모든 의존성 설치 (개발용 포함)
RUN npm ci

# 소스 코드 복사
COPY . .

# 환경변수 파일 복사
COPY .env .

# 개발 서버 실행 (Hot reload 지원)
EXPOSE 3000
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
